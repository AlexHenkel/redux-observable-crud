{
  "name": "redux-observable-crud",
  "version": "1.0.0",
  "main": "dist/redux-observable-crud.js",
  "scripts": {
    "build": "npm run clean && npm run build:js",
    "build:js": "babel src --presets babel-preset-env --out-dir dist",
    "prepublish": "npm run build",
    "clean": "rimraf dist"
  },
  "repository": "https://github.com/alexhenkel/redux-observable-crud.git",
  "author": "<alehenkel17@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "ramda": "^0.24.1",
    "redux": "^3.7.2",
    "redux-observable": "^0.16.0",
    "rxjs": "^5.4.3"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "rimraf": "^2.6.1"
  }
}
